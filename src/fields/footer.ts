import { Field } from 'payload'

export const Email: Field = {
  name: 'email',
  label: 'Email',
  type: 'text',
  required: true,
}

export const Phone: Field = {
  name: 'phone',
  label: 'Phone',
  type: 'text',
  required: true,
}
